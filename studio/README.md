# WEGO Forklift News Studio

This is the hosted editorial workspace for WEGO news content.

```bash
npm install
npm run dev
npm run deploy
```

The deployed studio is for editors. Create one document per language and keep the same `Translation group ID` for every translation of the same article.
